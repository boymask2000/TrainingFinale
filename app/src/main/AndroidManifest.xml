<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boymask.training">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quiz1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".colors.ColorActivity"></activity>
        <activity android:name=".colors.ColorsParameterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".math.MathParametersActivity" />
        <activity android:name=".math.MathActivity" />
    </application>

</manifest>